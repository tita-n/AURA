<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- READ_CONTACTS: requested contextually, only when the user first attempts a
         contact-dependent action (PRD §9.8). App works fully without it. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- READ_CALENDAR: requested contextually when enabling the Next Event module. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- Required to start AlarmClock timer/alarm on TECNO/HiOS (HandleSetAlarmApiCalls
         declares android:permission="com.android.alarm.permission.SET_ALARM").
         Without this, SET_TIMER throws SecurityException: Permission Denial. -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- Local file search — scoped MediaStore access (media + documents the OS allows). -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Broad local file discovery (Phase 5): all-files access. This is a PERSONAL launcher,
         not distributed via Google Play, so MANAGE_EXTERNAL_STORAGE is appropriate and used
         for genuine local search. It is NEVER requested at install/startup. AURA only offers it
         (via a calm in-Command-Bar action) when a scoped search comes back empty, and fails
         gracefully if the user declines. No file:// Uri is ever exposed; results open via
         FileProvider content:// Uris. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Package visibility for launcher discovery (API 30+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Timer/Alarm execution must be queryable on API 30+; without this,
             resolveActivity() returns null due to package visibility, causing the
             executor to falsely fallback to just opening Clock without a timer. -->
        <intent>
            <action android:name="android.intent.action.SET_TIMER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="31"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="AURA"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Home application role (PRD §16): makes AURA eligible as default launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- AURA Media Notification Listener — MUSIC ONLY, narrow, privacy-preserving.
             This is NOT a notification subsystem: no list, history, grouping, priority,
             storage, or rendering. Its sole purpose is to act as an optional technical
             bridge so AURA can discover other apps' active media sessions + structured
             metadata for the single Music contextual surface. The system grants the bind
             permission only when the user explicitly enables it in Android settings.
             No <uses-permission> is declared; enabling happens via Settings. -->
        <service
            android:name="com.aura.platform.android.AuraMediaNotificationListenerService"
            android:label="AURA Music (media only)"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- FileProvider: opens filesystem files discovered by local search via content:// Uris
             (never raw file://). Grants read permission to the viewer the user picks. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/aura_file_paths" />
        </provider>
    </application>

</manifest>
